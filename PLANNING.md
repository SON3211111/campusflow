# CampusFlow — 기획 컨텍스트

## 프로젝트 개요

- **정식 명칭**: CAMPUS FLOW — AI 기반 협업 관리 플랫폼
- **목적**: 졸업 작품 / 캡스톤 디자인
- **타겟 사용자**: 대학생 팀 프로젝트 (캡스톤, 수업 과제, 팀 스터디)
- **핵심 가치**: AI가 업무를 자동 분해하고, 팀이 유연하게 배분·관리하는 협업 플랫폼
- **상세 기획** → `docs/기획서.md`

## 핵심 개념

실제 사용 흐름은 두 가지로 나뉨:

### 플로우 A — AI 분해 + Picking (메인 차별점)
> 프로젝트 초반, 뭘 해야 할지 막막할 때

```
AI에 프로젝트 설명 입력 → 분해하기 클릭
    ↓
AI 결과 즉시 DB 저장 (assignee_id = NULL — 아직 배분 전 상태)
    ↓
AiTaskPage에서 팀원들이 DB 로드 → 각자 원하는 태스크를 본인 슬롯에 드래그 (Picking)
    ↓
"보드로 보내기" → assignee_id 확정, 칸반 보드에 반영
```

- 분해 완료 즉시 DB 저장 → **팀원이 언제 AiTaskPage 들어와도 같은 결과를 봄**
- 같은 태스크 중복 선택 방지: `is_locked` + `locked_by` DB 처리
- "Pool로 보내기" 버튼 불필요 — 분해 시점에 이미 서버에 있음

### 플로우 B — 직접 추가 (보조)
> 진행 중 추가 업무 발생 시, AI 결과 보완할 때

```
팀원이 칸반 보드에서 카드 직접 생성 → 본인 배정
```

- AI 없이 바로 카드 추가. 담당자는 생성 시 지정.
- 기존 협업툴(Jira, Trello)과 동일한 방식

## 타겟 사용자 페인포인트

대학생 팀 프로젝트에서 자주 겪는 문제:
- 무엇을 해야 할지 처음에 막막함 → AI 태스크 분해로 해결
- 역할 분담이 불명확하고 구두로만 결정됨 → 칸반 보드 + 담당자 배정
- 누가 뭘 하고 있는지 팀장이 파악하기 어려움 → 대시보드
- Notion/Jira 같은 툴은 배우기 복잡함 → 간단한 UX

## 핵심 기능 우선순위

| 순위 | 기능 | 이유 |
|---|---|---|
| 1 | AI 태스크 분해 | 메인 차별점. 타 서비스에 없는 핵심 기능 |
| 2 | 칸반 보드 | 기본 협업 인프라 |
| 3 | 대시보드 (팀 진척 확인) | 팀장 입장에서 필수 |
| 4 | 멤버 초대/관리 | 팀 협업 기본 요건 |
| 5 | 알림 | 마감일 임박 등 리마인드 |

## 졸업 작품 평가 기준 (추정)

일반적인 캡스톤 평가 항목:
- **완성도**: 핵심 기능이 실제로 동작하는가
- **독창성**: 기존 서비스와 다른 점이 있는가 (AI 태스크 분해)
- **기술 수준**: 스택 구성, 아키텍처 설계
- **UI/UX**: 사용하기 편한가
- **발표/시연**: 데모가 매끄럽게 돌아가는가

## 현재 완성도 자가 평가

| 항목 | 상태 | 비고 |
|---|---|---|
| AI 태스크 분해 | ✅ 동작 | 핵심 차별점, 완성됨 |
| 칸반 보드 (기본 CRUD) | ✅ 동작 | 드래그앤드롭, 소프트 삭제 |
| 직접 태스크 추가 | ✅ 동작 | 보드에서 카드 직접 입력 |
| 멤버 관리 | ✅ 동작 | 초대, 권한 구분 |
| 대시보드 | ⚠️ 부분 | 태스크 카운트 동작, 기여도·활동로그 미완 |
| Picking (AiTaskPage에서 팀원이 직접 드래그) | ❌ 미구현 | 본인 슬롯에만 드래그, 서버 저장 선행 필요 |
| 내 태스크 필터 (보드 상단 토글) | ❌ 미구현 | 별도 페이지 없이 토글 버튼으로 해결 |
| 카드 UI (담당자·마감일·우선순위) | ❌ 미구현 | 제목만 보임 |
| 마감 임박 강조 | ❌ 미구현 | 마감일 지나도 카드에 변화 없음 |
| 병목 태스크 감지 | ❌ 미구현 | 오래 멈춘 태스크 아무도 모름 |
| DONE 즉시 반응 (기여도+피드) | ❌ 미구현 | DONE 해도 대시보드에 아무 반응 없음 |
| 댓글 | ❌ UI만 | 저장 안 됨, 새로고침하면 사라짐 |
| 활동 로그 | ❌ 로컬만 | localStorage, 팀원 간 미공유 |
| 알림 UI | ❌ 미연결 | 백엔드만 완성 |
| Community 채팅 | ❌ 미구현 | 탭 전환 방식으로 변경, team_communication 연결 필요 |
| Planner 마감일 연결 | ❌ 하드코딩 | "마감일이 없습니다" 고정 출력 |
| 실시간 동기화 | ❌ 미구현 | 새로고침 해야 팀원 변경사항 보임 |

## 기획 피드백 시 고려 기준

Claude가 기획 관련 피드백을 줄 때 아래 기준으로 판단할 것:

1. **졸업 작품 완성도 우선** — 모든 기능을 완벽히 구현하기보다 핵심 기능이 시연 가능한 수준인지
2. **AI 차별점 강화** — AI 관련 기능(태스크 분해, 세부 분할)은 우선 완성도를 높일 것
3. **데모 가능성** — 발표 시연 시 막히지 않을 플로우인지 (댓글 저장 안 되면 시연 시 어색함)
4. **과도한 기능 추가 경계** — 새 기능보다 기존 미완성 기능 완성이 우선
5. **팀 규모 고려** — 소규모 팀(2~4인 추정)이 사용하는 시나리오 기준으로 평가

## 시연 시나리오 (핵심 플로우)

졸업 작품 발표에서 보여줄 핵심 흐름:
1. 워크스페이스 생성 → 팀원 초대
2. AI에 프로젝트 설명 입력 → 태스크 자동 분해 ← **차별점**
3. AI 분해 즉시 DB 저장 → AiTaskPage에서 팀원들이 각자 원하는 태스크를 본인 슬롯에 드래그 (Picking) ← **차별점**
4. 보드에서 "내 태스크만" 토글 → 본인 업무 집중 관리, 상태 업데이트
5. 대시보드에서 팀 기여도 + 병목 태스크 확인

## 일정

- **최종 발표**: 2026-06-30
- **남은 기간**: 약 29일 (2026-06-01 기준)
- **목표**: 웹사이트 구현 완료 + 시연 가능한 상태

## 6/30 기준 구현 우선순위

> 남은 기간 약 29일. 새 기능 추가보다 기존 미완성 기능 완성이 우선.

| 순위 | 기능 | 이유 |
|---|---|---|
| 1 | 카드 UI (담당자·마감일·우선순위·마감강조) | 가장 눈에 띄는 변화, 반나절 |
| 2 | DONE 즉시 반응 (기여도+활동피드) | 교수님 "수동적" 피드백 직접 해결 |
| 3 | AiTaskPage Picking 구조 전환 | 팀원이 직접 드래그, 중복 방지 |
| 4 | 댓글 DB 저장 | 발표 시연 시 쓰면 사라지는 문제 |
| 5 | 활동 로그 팀 공유 | "살아있는 협업" 느낌 |
| 6 | 알림 UI 연결 | 백엔드 완성, 프론트 연결만 |
| 7 | Planner 마감일 실제 연결 | 하드코딩 제거, 반나절 |
| 8 | 병목 태스크 감지 | 대시보드 완성도 |
| 9 | WebSocket 실시간 동기화 | 기술 완성도, 여유 있으면 |

## 교수님 피드백 (중간 시연)

> "협업툴로서의 task 관리가 명확히 보이지 않는다"
> "지속적으로 관리해주는 느낌이 없다"

**핵심 문제 — 툴이 수동적(passive)이다:**
지금 CampusFlow의 태스크 흐름: `AI 태스크 생성 → 보드에 올라감 → 드래그로 상태 바꿈 → 끝`
그 이후로 툴이 아무것도 안 함. 사람이 직접 다 확인하고 챙겨야 하면 협업툴이 아니라 게시판임.

| 상황 | 지금 CampusFlow | 진짜 협업툴 |
|---|---|---|
| 마감일이 내일인데 아직 TODO | 아무 일도 없음 | 카드 빨간 강조 + 알림 |
| 팀원이 3일째 DOING에서 안 움직임 | 아무도 모름 | 대시보드에 병목으로 표시 |
| 태스크를 DONE으로 바꿈 | 그냥 이동됨 | 기여도 +1, 활동 피드에 기록 |
| 댓글이 달림 | 저장도 안 됨 | 담당자에게 알림 |

**이 피드백을 기준으로 기능 평가 시:** "이 기능이 툴을 능동적으로 만드는가 — 툴이 팀을 대신해서 챙겨주는가"를 우선 판단할 것.

### 반드시 완료 (협업툴답게 보이기 위한 핵심)
1. **AiTaskPage Picking 구조로 전환** — 팀원들이 AI 결과에서 본인 슬롯에 직접 드래그. 서버 저장 + 폴링으로 중복 선택 방지. 별도 TaskPoolPage 없음.
2. **칸반 카드에 담당자 아바타 표시** — assigneeId DB에 있음, 카드 UI에 표시만 추가. "누가 하는지" 바로 보임
3. **칸반 카드에 마감일 + 우선순위 표시** — "언제까지, 얼마나 급한지" 카드만 봐도 파악 가능
4. **마감 임박 카드 자동 강조** — D-3 이내 카드 빨간 테두리, DONE 제외. 툴이 능동적으로 경고
5. **DONE 처리 시 즉시 반응** — ContributionMetrics 카운트 증가 + 활동 피드에 "OOO이 완료" 기록. 지금은 DONE 해도 아무 일 없음
6. **병목 태스크 감지** — ISSUE/DOING 상태에 3일 이상 머문 태스크를 대시보드에 강조 표시
7. **활동 로그 백엔드 저장** — 팀원 활동이 실시간으로 기록되어 "살아있는 협업" 느낌. 지금은 localStorage라 나만 봄
8. **댓글 저장** — UI는 있는데 DB 저장 안 됨. 발표 시연 시 댓글 쓰면 사라짐
9. **알림 UI** — 백엔드 API 완성됨. 상태변경/댓글/마감임박 시 헤더 벨 아이콘에 표시
10. **Planner 캘린더 실제 연결** — "마감일이 없습니다" 하드코딩 제거, tasks의 dueDate 연결

### 하면 좋음 (시간 되면)
- Task 제목 수정 기능
- AI priority/estimated_hours 저장 및 표시
- 커스텀 컬럼 영구 저장 (board_columns 테이블)
- **공강 시간 추천** — 수동 입력 방식으로 구현 (이미지 파싱 X). user_schedules + schedule_blocks DB 저장 → 팀원 공통 빈 시간 추천. 대학생 타겟 실용 기능.
- **교수 피드백** — 교수 역할 계정으로 학생 프로젝트에 피드백. 발표 시 교수님 앞 시연 임팩트 큼.

### WebSocket 실시간 동기화 — 구현 확정
Task 상태 변경 시 팀원 화면에 즉시 반영. Spring Boot STOMP + SockJS. REQ-N-01 충족.
핵심 플로우 완성 후 3단계에서 추가 (기존 REST 위에 얹는 구조라 기존 코드 안 깨짐).

### 스킵 확정
- 파일 첨부 (AWS S3) — 세팅 비용 대비 임팩트 없음
- Notion 내보내기 — 핵심과 무관
- AI 추천 (ai_recommendations) — 데이터 없으면 의미 없음
- 실시간 번역 (Papago) — 범위 초과
- 시간표 이미지 파싱 — 수동 입력으로 대체
- FCM 푸시 알림 — SSE로 대체 가능

## 업데이트 이력

- 2026-05-28 초안 작성, 발표 일정 추가
- 2026-06-01 교수님 "지속적 관리 느낌 없음" 피드백 반영, 완성도 평가 전면 업데이트, 시연 시나리오 수정, 6/30 우선순위 추가
- 2026-06-01 핵심 개념 재정리 — 업무 생성(AI분해/직접추가) + 업무 배분(Picking) 구조 확정
- 2026-06-01 직접 할당 제거, Community 탭 전환 방식 결정, My Task → 보드 토글 필터로 변경, 시연 시나리오 최종 확정
