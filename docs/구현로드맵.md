# 구현 로드맵

**최종 발표일**: 2026-06-30 (남은 기간 약 33일)
**구현 방식**: B방식 — 현재 Workspace→Task 구조 유지 + 필요한 테이블/필드 추가

---

## 브랜치 전략

```
main      ← 안정 배포 브랜치
dev       ← 팀 공용 브랜치 (건드리지 않음)
develop   ← 통합 브랜치 (Claude Code 작업 기준점)
  ├── feature/stage1-task-system   → develop에 머지
  ├── feature/stage2-completion    → develop에 머지
  └── feature/stage3-tech         → develop에 머지
```

**규칙:**
1. `dev` 기준으로 `develop` 브랜치 생성
2. 기능 작업 시 `develop`에서 `feature/xxx` 브랜치 생성
3. 기능 완료 시 `develop`에 머지

---

## 전체 단계 요약

```
1단계 DB + 핵심 플로우     약 10일
2단계 완성도               약  7일
3단계 기술 완성도          약  6일
마무리                     약  6일
─────────────────────────────────
합계                       약 29~31일 (33일 안에 완료 가능)
```

---

## 1단계 — DB + 핵심 플로우 (약 10일)

> Task Pool → Picking → My Task → Dashboard 플로우 완성
> 이게 없으면 발표 불가

### DB 작업 (1일)

**tasks 테이블 필드 추가:**
- `priority` varchar (HIGH/MEDIUM/LOW)
- `is_locked` boolean default false
- `locked_by` varchar (동시 Picking 방지)
- `quick_signal` varchar
- `ai_generated_data` json
- `column_id` varchar (board_columns FK)

**새 테이블 3개 추가:**
- `board_columns` — 커스텀 컬럼 DB 저장 (column_id, project_id, name, position)
- `task_status_history` — 활동 로그 (history_id, task_id, prev_status, curr_status, changed_by, occurred_at)
- `team_communication` — 댓글/채팅 (message_id, project_id, task_id, sender_id, parent_msg_id, content, mention_list, created_at)

**user_schedules + schedule_blocks 추가** (공강 시간 추천용):
- `user_schedules` — schedule_id, user_id, updated_at
- `schedule_blocks` — block_id, schedule_id, category, title, day_of_week, start_time, end_time

### Task Pool 화면 + Picking UI (3일)
- `assignee_id IS NULL` = Pool 상태
- 팀원이 Pool에서 태스크 선택 → `is_locked = true` → assignee 할당
- 동시 선택 방지: `is_locked` + `locked_by` DB 레벨 처리
- 새 화면: TaskPoolPage.tsx

### My Task 개인 보드 (2일)
- 본인 assignee 태스크만 표시
- 상태 변경 → team 대시보드에 반영
- 새 화면: MyTaskPage.tsx

### Team Board 전체 조회 (1일)
- 모든 팀원 태스크 + 담당자별 그룹
- 기존 WorkSpacePage 확장 또는 새 탭

### 카드 UI 개선 (1일)
- 칸반 카드에 담당자 아바타 표시
- 마감일 표시
- 우선순위 배지 (🔴HIGH / 🟡MEDIUM / 🟢LOW)
- 마감 임박 카드 빨간 테두리 강조

### 개인 보드 → 대시보드 동기화 (2일)
- ContributionMetrics: DONE 처리 시 task_completion_count 증가
- 대시보드 기여도 실제 데이터 반영

---

## 2단계 — 완성도 (약 7일)

> 협업툴로서 기본 기능 완성

- **AI 백그라운드 처리 + 토스트 알림** (0.5일)
  - 현재: 프롬프트 입력 → 검은 로딩 화면에서 최대 120초 대기
  - 변경: 모달 닫힘 → 기존 화면 유지 → 헤더에 "🤖 분석 중..." 표시 → 완료 시 토스트 "결과 보기"
  - React Context로 AI 로딩 상태 전역 관리
- **댓글 저장** (2일) — team_communication 테이블 연결, UI → API → DB
- **활동 로그** (2일) — task_status_history 기록, 대시보드 활동 피드에 표시
- **알림 UI 연결** (1일) — 백엔드 API 완성됨, 프론트 연결만
- **소셜 로그인 OR 동시성 제어** (1일) — OAuth 또는 is_locked 기반 Picking 락 완성
- **마감 임박 색상 강조** (0.5일) — 카드 배경/테두리 Red (DONE 제외)

---

## 3단계 — 기술 완성도 (약 6일)

> 교수님 기술 평가 포인트

- **WebSocket 실시간 동기화** (3일)
  - Spring Boot STOMP + SockJS
  - Task 상태 변경 시 팀원 화면 즉시 반영
  - 기존 REST 위에 얹는 구조 (기존 코드 안 깨짐)
  - REQ-N-01 충족

- **검색/필터** (2일)
  - Task 검색, 상태별/담당자별 필터
  - Task Pool 화면에 우선 적용

- **공강 시간 추천** (2일) ← 시간 여유 있을 때
  - 팀원 시간표 수동 입력 (이미지 파싱 X)
  - 공통 빈 시간 계산 → 상위 3개 추천
  - Calendar 탭에 표시

---

## 마무리 (약 6일)

- **버그 수정 + 테스트** (4일) — 핵심 플로우 전체 시나리오 테스트
- **시연 데이터 준비** (1일) — 발표용 워크스페이스 세팅, 테스트 계정 준비
- **산출물 통일** (1일) — 문서 용어 통일 (Ollama 통일, Task Pool/Picking 용어 통일)

---

## 스킵 확정

| 기능 | 이유 |
|---|---|
| 파일 첨부 (AWS S3) | 세팅 비용 대비 발표 임팩트 없음 |
| Notion 내보내기 | 핵심과 무관 |
| AI 추천 | 데이터 없으면 의미 없음 |
| 실시간 번역 | 범위 초과 |
| 시간표 이미지 파싱 | 수동 입력으로 대체 |
| FCM 푸시 알림 | SSE로 대체 가능 |

---

## 시연 핵심 플로우 (발표 시나리오)

```
1. 워크스페이스 생성 → 팀원 초대
2. AI에 프로젝트 설명 입력 → 태스크 자동 분해
3. Task Pool 생성됨
4. 팀원 각자 원하는 태스크 선택 (Picking)
5. My Task 보드에서 상태 관리 (드래그앤드롭)
6. 팀원 B가 상태 변경 → 팀원 A 화면에 실시간 반영 (WebSocket)
7. Dashboard에서 팀 진척 + 기여도 확인
```

---

---

## 추후 구현 (1~3단계 완료 후)

> 로드맵 완성 이후 완성도와 차별점을 높이는 기능들
> ERD에 이미 설계된 테이블/필드 활용 가능

### 발표 임팩트 높음

| 기능 | 설명 | 근거 | 난이도 |
|---|---|---|---|
| **교수 피드백** | 교수 계정으로 학생 프로젝트에 피드백 작성. OBSERVER 역할 활용. 발표 시 교수님 앞 시연 임팩트 큼 | ERD `academic_feedbacks` 설계됨 | 중간 |
| **병목 구간 분석** | ISSUE/DOING 상태에 오래 머문 태스크 강조. 대시보드에 추가 | `task_status_history` 데이터 활용 | 중간 |
| **퀵 시그널 (핑)** | 카드 내 "도움 요청 / 피드백 요청" 원클릭 → 팀원 알림. 협업 차별점 | DB `quick_signal` 필드 예정 | 쉬움 |

### 완성도

| 기능 | 설명 | 근거 | 난이도 |
|---|---|---|---|
| **태스크 제목 수정** | 현재 설명/마감일만 수정 가능. 기본 CRUD 누락 | - | 쉬움 |
| **멘션(@) UI** | 댓글 작성 시 @팀원 멘션 → 알림 연동 | ERD `mention_list json` 설계됨 | 중간 |
| **스레드 답글** | 댓글에 대댓글 구조 | ERD `parent_msg_id` 설계됨, REQ-F-08 | 중간 |
| **이슈 트래킹 게시판** | CHAT/ISSUE 구분해서 이슈만 모아보는 뷰 | ERD `msg_type ISSUE`, REQ-F-11, IA | 중간 |
| **프로젝트 상태 관리** | PENDING/PROGRESS/COMPLETED 상태 UI 표시/변경 | ERD `project_status_enum` | 쉬움 |
| **Task Pool에서 AI 추가 생성** | Task Pool 화면에서 AI로 태스크 추가 생성 버튼 | IA 명시 | 쉬움 |

### UX 완성도

| 기능 | 설명 | 근거 | 난이도 |
|---|---|---|---|
| **Empty State UX** | Task 없을 때 안내 메시지 + CTA 버튼 (Pool/My Task 화면) | REQ-U-02 | 쉬움 |
| **Task 완료 컨페티 효과** | DONE 처리 시 컨페티 애니메이션 (Framer Motion) | 기능명세서 7.1.2 | 매우 쉬움 |
| **다크모드** | 시스템 다크모드 연동 | 기능명세서 7.1.2 | 중간 |

---

## 업데이트 이력

- 2026-05-28 초안 작성, 추후 구현 섹션 추가
